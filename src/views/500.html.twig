{% extends "layout.html.twig" %}

{% block meta_title %}{{ meta.title }} - {{ parent() }}{% endblock %}

{% block content %}
         <header class="page-header">
            <h1>{{ page.title }}</h1>
         </header>
         <article class="row">
            <div class="col-md-12">
               {{ page.content }}
            </div>
         </article>
{% endblock %}